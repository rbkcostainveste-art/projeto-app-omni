const {chromium}=require('C:/Users/rbkco/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const assert=require('node:assert/strict');
(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});try{for(const width of [390,1366]){const p=await b.newPage({viewport:{width,height:900}});const errors=[];p.on('pageerror',e=>errors.push(e.message));await p.goto('http://localhost:3011/crew-scheduling-test',{timeout:120000});await p.getByRole('combobox').first().waitFor();console.log(await p.getByRole('combobox').first().getAttribute('aria-label'));await p.getByRole('combobox').first().fill('PR-QSC');await p.getByRole('option',{name:'PR-QSC · S92',exact:true}).click();const pilot=p.getByLabel('Comandante',{exact:true});await pilot.waitFor();assert.deepEqual(await pilot.locator('option').allTextContents(),['Selecione','Da base S92 · P1']);assert.deepEqual(await p.getByLabel('Copiloto',{exact:true}).locator('option').allTextContents(),['Selecione','Da base S92 · P1']);assert.deepEqual(await p.getByLabel('Comissário',{exact:true}).locator('option').allTextContents(),['Selecione','Comissário da base · C1']);assert.deepEqual(errors,[]);console.log('PASS base and fleet scheduling',width);await p.close();}}finally{await b.close();}})().catch(e=>{console.error(e);process.exitCode=1});

