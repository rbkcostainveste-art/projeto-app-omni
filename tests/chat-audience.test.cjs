const {test}=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const ts=require('typescript');const lib={};new Function('exports',ts.transpileModule(fs.readFileSync('src/lib/chat-audience.ts','utf8'),{compilerOptions:{module:ts.ModuleKind.CommonJS}}).outputText)(lib);
const people=[{id:'1',name:'Inspetor A',base:'A',fleets:['AW139'],role:'maintenance_inspector',workShift:'night',mission:'mission_2'},{id:'2',name:'Inspetor B',base:'B',fleets:['S92'],role:'maintenance_inspector',workShift:'day',mission:'mission_1'},{id:'3',name:'Mecânico',base:'B',role:'mechanic'}];
test('combines criteria inside blocks and unions blocks without duplicates',()=>{const a={...lib.emptyAudienceRule(),base:'A',fleet:'AW139',shift:'night',mission:'mission_2',role:'maintenance_inspector'};const b={...lib.emptyAudienceRule(),role:'maintenance_inspector'};assert.deepEqual(lib.audiencePeople(people,{rules:[a,b,a],included:[],excluded:[]}).map(p=>p.id),['1','2']);});
test('manual additions and exclusions persist across changed matching rules',()=>{const rule={...lib.emptyAudienceRule(),role:'maintenance_inspector'};assert.deepEqual(lib.audiencePeople(people,{rules:[rule],included:['1','3'],excluded:['1']}).map(p=>p.id),['2','3']);assert.deepEqual(lib.audiencePeople(people,{rules:[],included:['3'],excluded:[]}).map(p=>p.id),['3']);});
test('an empty audience sends to nobody',()=>assert.deepEqual(lib.audiencePeople(people,{rules:[],included:[],excluded:[]}),[]));
