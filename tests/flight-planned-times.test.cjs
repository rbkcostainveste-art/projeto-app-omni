const {test}=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const ts=require('typescript');const lib={};new Function('exports',ts.transpileModule(fs.readFileSync('src/lib/flight-planned-times.ts','utf8'),{compilerOptions:{module:ts.ModuleKind.CommonJS,target:ts.ScriptTarget.ES2022}}).outputText)(lib);
test('commander and copilot edit only assigned flights; coordination stays in base',()=>{const f={commander:'P1',copilot:'P2',base:'A'};assert.equal(lib.canEditPlannedTimes('commander','P1',f,''),true);assert.equal(lib.canEditPlannedTimes('copilot','P2',f,''),true);assert.equal(lib.canEditPlannedTimes('copilot','P3',f,''),false);assert.equal(lib.canEditPlannedTimes('flight_attendant','P1',f,'A'),false);assert.equal(lib.canEditPlannedTimes('coordination','C',f,'A'),true);assert.equal(lib.canEditPlannedTimes('coordination','C',f,'B'),false);});
test('arrival changes duration including midnight and rejects invalid times',()=>{assert.equal(lib.durationFromArrival('23:30','01:00'),1.5);assert.equal(lib.minutesClock(lib.timeMinutes('23:30')+90),'01:00');assert.equal(Number.isNaN(lib.timeMinutes('99:00')),true);});
