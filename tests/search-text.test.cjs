const {test}=require('node:test');const assert=require('node:assert/strict');const ts=require('typescript');const fs=require('node:fs');const lib={};new Function('exports',ts.transpileModule(fs.readFileSync('src/lib/search-text.ts','utf8'),{compilerOptions:{module:ts.ModuleKind.CommonJS}}).outputText)(lib);
test('aircraft searches accept short, unpunctuated and mixed-case prefixes',()=>{for(const q of ['ch','CHT','prcht','PR CHT'])assert.equal(lib.matchesSearch('PR-CHT · S92',q),true);assert.equal(lib.matchesSearch('PR-CGO · AW139','cht'),false)});
test('search accepts accents and multiple terms while retaining option scope',()=>{const options=['PR-CHT S92 Jacarepaguá','PR-CGO AW139'];assert.deepEqual(options.filter(v=>lib.matchesSearch(v,'jacare cht')),[options[0]]);assert.equal(lib.matchesSearch(options[0],''),true);assert.equal(lib.matchesSearch(options[0],'AW139'),false)});
