export const flightProposalTool={type:'function',name:'preparar_voos',description:'Prepara lançamentos de voos para conferência humana. Não salva. Preserve null quando faltar dado. Consulte fleet para identificar prefixos e base. Não invente duração, combustível ou horário.',strict:true,parameters:{type:'object',properties:{flights:{type:'array',items:{type:'object',properties:{prefix:{type:['string','null']},base:{type:['string','null']},date:{type:['string','null']},departure:{type:['string','null']},duration:{type:['number','null']},fuelAmount:{type:['number','null']},fuelUnit:{type:['string','null'],enum:['L','lb','kg',null]}},required:['prefix','base','date','departure','duration','fuelAmount','fuelUnit'],additionalProperties:false}}},required:['flights'],additionalProperties:false}};
export function parseFlightProposals(value:unknown){
 if(!Array.isArray(value)||value.length>30)throw Error('Até 30 voos por pedido.');
 return value.map(v=>{if(!v||typeof v!=='object')throw Error('Voo inválido.');for(const k of ['prefix','base','date','departure'])if(v[k]!==null&&(typeof v[k]!=='string'||v[k].length>100))throw Error('Campo inválido.');for(const k of ['duration','fuelAmount'])if(v[k]!==null&&(typeof v[k]!=='number'||!Number.isFinite(v[k])||v[k]<0))throw Error('Quantidade inválida.');if(!['L','lb','kg',null].includes(v.fuelUnit))throw Error('Unidade inválida.');if(v.date&&!/^\d{4}-\d{2}-\d{2}$/.test(v.date)||v.departure&&!/^([01]\d|2[0-3]):[0-5]\d$/.test(v.departure))throw Error('Data/horário inválido.');return {prefix:v.prefix,base:v.base,date:v.date,departure:v.departure,duration:v.duration,fuelAmount:v.fuelAmount,fuelUnit:v.fuelUnit};});
}
